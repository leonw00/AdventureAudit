# AdventureAudit Client

AdventureAudit frontend(client) using React.

# Getting Started

Install dependencies:

```
yarn install
```

Create `.env` file and put api keys into the `.env` file:

```
cp .env.sample .env
```

Run the application:

```
yarn start
```

## Running Using Docker

```
docker-compose up
```